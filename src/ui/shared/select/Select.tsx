import { StandardTextFieldProps, SxProps, TextField, Theme } from "@mui/material"


export const Select = (props:StandardTextFieldProps) =>{

    return (
        <TextField {...props}>
            
        </TextField>
    )
}